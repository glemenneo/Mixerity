export enum OrderBy {
    ASC = 'ASC',
    DESC = 'DESC',
}

export enum UserColumns {
    UID = 'uid',
    EMAIL = 'email',
    USERNAME = 'username',
    CREATED = 'created',
}

export enum ProfileColumns {
    UID = 'uid',
    DISPLAYNAME = 'displayName',
}
